"""Notification delivery package — Phase 3.

Builds notification lists from approved ``NotificationSubject`` rows,
delivers via SMTP email or print-ready PDF letters, and logs every
delivery event to the audit trail.
"""
